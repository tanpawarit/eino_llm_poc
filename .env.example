# ===================================
# API Configuration
# ===================================
# OpenRouter API key for LLM model access
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Redis Configuration (Upstash or other Redis provider)
REDIS_URL=your_redis_connection_string_here

# ===================================
# NLU Configuration
# ===================================
# LLM model to use for NLU processing
NLU_MODEL=openai/gpt-3.5-turbo

# Maximum tokens for NLU model responses
NLU_MAX_TOKENS=2000

# Temperature for NLU model (0.0 to 1.0, lower = more deterministic)
NLU_TEMPERATURE=0.1

# Importance threshold for filtering NLU results (0.0 to 1.0)
NLU_IMPORTANCE_THRESHOLD=0.6

# Default intent definitions with confidence scores
NLU_DEFAULT_INTENT=greet:0.1, purchase_intent:0.8, inquiry_intent:0.7, support_intent:0.6, complain_intent:0.6

# Additional intent definitions with confidence scores
NLU_ADDITIONAL_INTENT=complaint:0.5, cancel_order:0.4, ask_price:0.6, compare_product:0.5, delivery_issue:0.7

# Default entity types to extract
NLU_DEFAULT_ENTITY=product, quantity, brand, price

# Additional entity types to extract
NLU_ADDITIONAL_ENTITY=color, model, spec, budget, warranty, delivery

# ===================================
# Conversation Management Configuration
# ===================================
# Time-to-live for conversations in seconds
CONVERSATION_TTL=15

# Maximum number of turns to include in NLU context
CONVERSATION_NLU_MAX_TURNS=5

# Maximum number of turns to include in response context
CONVERSATION_RESPONSE_MAX_TURNS=10
package model

// session:{session_id}:graph          // Graph metadata
// session:{session_id}:nodes:{node_id} // Node data
// session:{session_id}:state:{node_id} // Node execution state
// session:{session_id}:edges          // Adjacency data (hash/set)
// session:{session_id}:current        // Current execution position